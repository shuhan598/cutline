# 切线算法与数据问题清单

> 更新时间：2026-07-17
>
> 本文档记录当前代码、数据契约、接口对接和上线闭环中仍需处理的问题。旧版文档中“溢满 / 切回 / 丝网 / 混料 / 逐台选取未实现”的结论已经过期：当前主链路已包含这些模块，并已通过自动化测试覆盖。

## 当前结论

当前正式调用链为：

```text
CutlineAlgorithmRequest
  -> SnapshotAdapter.to_algorithm_snapshot
  -> CutlinePipeline.evaluate_algorithm
  -> AlgorithmResponseMapper.to_response
  -> CutlineAlgorithmResponse
```

算法已具备以下能力：

- 后端请求模型校验与快照转换。
- 机台状态标准化：后端“运行”/`running` 进入算法统一为 `running`，其它状态统一视为非运行。
- Buffer 区间净消耗速率计算。
- 断料预测、断料预警。
- 溢满预测、溢满预警。
- 断料/溢满候选机台筛选。
- 逐台影响校验和切线方案生成。
- 人工介入结果输出。
- 活动切线事件生成与切回判断。
- 丝网当前订单完工与清台准备提醒。
- 混料追溯记录与失败原因输出。
- 对外响应对象 `CutlineAlgorithmResponse`。
- 完整假数据样例：`examples/backend_request_stockout_plan_sample.json`。
- 完整响应样例：`debug_outputs/stockout_plan_sample_response.json`。

## 问题状态速览

| 编号 | 问题 | 严重程度 | 当前状态 |
|---|---|---|---|
| P1 | 尚无正式 HTTP 接口 | 高 | 待实现 |
| P2 | 真实后端数据尚未联调 | 高 | 待联调 |
| P3 | 净速率口径需业务确认 | 高 | 待确认 |
| P4 | 后端活动事件持久化闭环未落地 | 高 | 待后端对接 |
| P5 | 对外请求接口文档仍需补齐 | 中 | 待补文档 |
| P6 | 部分业务规则未定版 | 中 | 待确认 |
| P7 | MES 混料推送闭环未实现 | 中 | 待后续系统集成 |
| P8 | 生产级日志、监控和错误码不足 | 中 | 待工程化 |
| P9 | 文档与代码状态曾不一致 | 中 | 本文档已更新，仍需持续维护 |
| P10 | Git/本地环境存在 `.pytest_cache` 权限警告 | 低 | 不影响测试结果，待清理环境 |

## P1. 尚无正式 HTTP 接口

当前项目只有服务层入口：

```python
CutlineService().evaluate_algorithm(request)
```

`app/api/` 与 `app/main.py` 目前没有可供外部调用的正式 HTTP 路由。因此后端现在不能直接通过接口访问算法，只能在 Python 进程内调用服务类。

建议实现：

```http
POST /api/cutline/evaluate
Content-Type: application/json
```

请求体使用 `CutlineAlgorithmRequest`，响应体使用 `CutlineAlgorithmResponse`。

## P2. 真实后端数据尚未联调

当前已有完整假数据样例，但还需要使用甲方真实接口数据验证以下内容：

- 顶层数据集是否齐全。
- 字段名是否完全匹配 `CutlineAlgorithmRequest`。
- 机台状态枚举是否只需支持“运行”/`running` 作为正常状态。
- 订单名称 `order_name` 是否一定非空。
- Buffer 与工序路线是否能唯一推导相邻工序关系。
- 库存订单是否能通过 `bound_source_name` 匹配订单名称。
- 机台、产线、订单、产品、产能表之间的引用是否完整。
- `machine_process_times` 是否包含混料追溯需要的工艺时间。

风险：真实数据只要缺少关键引用，算法会按设计抛出校验或转换错误，无法进入完整计算。

## P3. 净速率口径需业务确认

当前代码中的净消耗速率按机台实时 30 分钟输入/输出数量折算小时速率：

```text
上游产出速率 = 上游运行机台 output_quantity_30m 之和 * 2
下游吞入速率 = 下游运行机台 input_quantity_30m 之和 * 2
净消耗速率 = 下游吞入速率 - 上游产出速率
```

这与部分设计文档中“运行机台按静态表实际产能计算有效产能”的口径不同。

需要业务确认：

- 净速率到底以实时 30 分钟数据为准，还是以静态表实际产能为准。
- 若实时数据异常或长时间不刷新，是否允许回退到静态产能。
- 当前 30 分钟数量乘 2 的小时化方式是否符合现场统计周期。

## P4. 后端活动事件持久化闭环未落地

算法会输出两类跟踪事件：

- `new_active_cutline_events`
- `updated_active_cutline_events`

这些字段是给后端保存和下轮请求回传用的，不用于前端展示。

后端需要完成：

1. 保存 `new_active_cutline_events`。
2. 下轮请求时，把仍处于跟踪期的事件放入请求顶层 `active_cutline_events`。
3. 收到 `updated_active_cutline_events` 后，按 `event_id` 更新已保存事件。
4. 对已完成、已取消或已切回的事件做生命周期管理。

如果后端不保存这些事件，切回判断会丢失历史上下文。

## P5. 对外请求接口文档仍需补齐

当前已补充对外响应接口文档：

- `docs/backend-response-interface.md`

仍需补齐请求接口文档，建议覆盖：

- `CutlineAlgorithmRequest` 顶层结构。
- 每个数据集的字段说明。
- 哪些字段允许 `null`。
- 哪些字段允许空数组。
- `order_name` 不允许为空。
- 机台状态映射规则。
- Buffer 库存如何通过订单名称匹配订单。
- 活动事件 `active_cutline_events` 如何由后端回传。

## P6. 部分业务规则未定版

仍需业务或现场确认的规则包括：

- 片源兼容规则：例如 `A-`、`A` 的优先级，以及“A 不能切到 A”是否成立。
- S2 车间 R/P 共用机台特例的适用范围。
- 丝网前 R/P 是否始终视为兼容。
- 切线方案是否允许同 Buffer 内切换。
- 溢满场景下目标订单选择优先级。
- 多个预警同时触发时的最终排序策略是否符合现场调度习惯。

## P7. MES 混料推送闭环未实现

当前算法可以生成 `mixing_trace_records`，但不会主动推送 MES，也不会写数据库。

后续系统集成需要明确：

- 后端是否保存混料记录。
- 由后端还是算法服务推送 MES。
- MES 接口格式。
- 推送失败后的重试策略。
- `notification_status=scheduled/due` 的状态更新方式。

## P8. 生产级日志、监控和错误码不足

当前响应中已有 `errors`，但生产运行还需要更完整的工程能力：

- 每次请求的 `run_id` 贯穿日志。
- 结构化日志。
- 统一接口错误码。
- Schema 校验错误与算法局部错误分层。
- 关键指标监控：请求量、失败率、预警数量、方案数量、人工介入数量。
- 慢请求和异常告警。

## P9. 文档与代码状态曾不一致

旧版问题清单中保留了“逐台选取、溢满、切回、丝网、混料未实现”的说法，但当前代码已经包含这些模块。

已处理：

- 本文档已按当前代码状态重写。
- 对外响应接口文档已补充。

仍需注意：

- 后续代码变更后，应同步更新文档。
- 设计文档中的旧结论需要在对外沟通时谨慎引用。

## P10. 本地环境 `.pytest_cache` 权限警告

当前运行 Git 或 pytest 时，本地 `.pytest_cache` 目录可能出现权限警告：

```text
warning: could not open directory '.pytest_cache/': Permission denied
```

该问题不影响算法代码和测试结果，但会影响本地状态查看体验。建议后续清理本地缓存目录权限。

## 当前测试状态

最近一次全量测试结果：

```text
810 passed, 8 warnings
```

8 个 warning 为 Pydantic `model_fields` 访问方式的弃用警告，不影响当前功能。

## 建议下一步

1. 实现正式 HTTP 接口 `POST /api/cutline/evaluate`。
2. 补齐后端请求接口文档。
3. 用甲方真实数据做联调。
4. 明确净速率计算口径。
5. 明确片源和 R/P 兼容规则。
6. 落地后端活动事件持久化闭环。
7. 确认 MES 混料记录推送方案。
